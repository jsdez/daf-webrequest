{
  "name": "daf-webrequest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "bundle": "node build.mjs",
    "deploy": "npm run build && npm run bundle"
  },
  "keywords": [],
  "author": "DAF Trucks N.V.",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.0.1",
    "esbuild": "^0.25.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@nintex/form-plugin-contract": "^1.0.5",
    "lit": "^3.3.0"
  }
}
